let valorIngresso = 30; // let permite alterar / redefinir o valor da variável

valorIngresso = 25;

console.log(valorIngresso); // Será impresso no console o valor 25, ao invés de 30.

const valorIngressoCorreto = 20; // Deve ser utilizado quando se têm valores que não precisam ser redefinidos

console.log(valorIngressoCorreto);
